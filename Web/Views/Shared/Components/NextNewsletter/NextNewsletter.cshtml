@model NextNewsletterViewModel
@using Core.Models.Components
@using Core.Dtos.Newsletter
@using Core.Models.User

<div class="alert alert-info">
    <h5 class="mb-0">Upcoming Entry</h5>
    <h6 class="mb-1 font-weight--normal">
        @if (Model.TimeUntilNextSend.HasValue)
        {
            var totalHoursAsInt = Convert.ToInt32(Model.TimeUntilNextSend.Value.TotalHours);
            if (totalHoursAsInt <= 0)
            {
                <span>Your next entry is sending</span>
            }
            else
            {
                <span>Your next entry will send in @totalHoursAsInt hours</span>
            }
        }
    </h6>
</div>
